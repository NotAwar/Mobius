---
name: golangci-lint

on:
  push:
    branches:
      - main
      - patch-*
      - prepare-*
    paths:
      - '**.go'
      - 'go.work'
      - '**/go.mod'
      - '**/go.sum'
      - '.github/workflows/golangci-lint.yml'
  pull_request:
    paths:
      - '**.go'
      - 'go.work'
      - '**/go.mod'
      - '**/go.sum'
      - '.github/workflows/golangci-lint.yml'
  workflow_dispatch: # Manual

# This allows a subsequently queued workflow run to interrupt previous runs
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id}}
  cancel-in-progress: true

defaults:
  run:
    # fail-fast using bash -eo pipefail. See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions  # exit-codes-and-error-action-preference
    shell: bash

permissions:
  contents: read

jobs:
  golangci:
    permissions:
      contents: read  # for actions/checkout to fetch code
      pull-requests: read  # for actions/checkout to fetch pull requests
    name: lint
    strategy:
      fail-fast: false
      matrix:
        # See #9943, we just need to add windows-latest here once all issues are fixed.
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      # Tell git not to change line endings when checking out files on Windows. Without this gofmt
      # flags every single file on windows-latest
      - run: 'git config --global core.autocrlf input'


      - name: Checkout code
        uses: actions/checkout@71cf2267d89c5cb81562390fa70a37fa40b1305e # v5.0.0

      - name: Install Go
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version-file: 'server/api/go.mod'

      - name: Install dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          # The following packages are needed to build Mobius Desktop on Ubuntu.
          sudo apt update -y && sudo apt install -y gcc libgtk-3-dev libayatana-appindicator3-dev

      - name: Run golangci-lint on server
        uses: golangci/golangci-lint-action@v8
        with:
          version: latest
          working-directory: server/api
          args: --timeout=5m

      # CLI module linting disabled - contains legacy code that will be refactored
      # - name: Run golangci-lint on CLI
      #   uses: golangci/golangci-lint-action@v8
      #   with:
      #     version: latest
      #     working-directory: server/cli

      # Client module linting disabled - contains legacy code that will be refactored
      # - name: Run golangci-lint on client
      #   uses: golangci/golangci-lint-action@v8
      #   with:
      #     version: latest
      #     working-directory: client/client

      # - name: Run cloner-check tool
      #   run: |
      #     cd server/api && go run ./tools/cloner-check/main.go -check
