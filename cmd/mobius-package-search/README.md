# Package Search

This container runs in a Ubuntu container, with apt and flatpak installed. Since these don't have their own APIs, we're using the Ubuntu container to search, and return the search results as an API
